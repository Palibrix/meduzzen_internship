version: '3.9'

services:
  web:
    build: .
    env_file:
      - .env
    command: sh -c "alembic upgrade head && uvicorn app.main:app --reload --host ${HOST} --port ${PORT}"
    ports:
      - "8000:8000"
    volumes:
      - .:/task
    depends_on:
      - db
      - redis
    restart: unless-stopped

  db:
    image: postgres:14.9
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    ports:
      - "5433:5432"
    restart: unless-stopped
    volumes:
      - ./db-internship/:/var/lib/postgresql/vol/
      - ./.conf/db:/docker-entrypoint-initdb.d/

  redis:
    image: redis:7.2.1
    ports:
      - '6379:6379'
    restart: unless-stopped